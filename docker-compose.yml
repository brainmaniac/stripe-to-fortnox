services:
  app:
    build: .
    container_name: stripe-fortnox-sync
    restart: unless-stopped
    ports:
      - "80:8080"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - APP_PORT=8080
      - DB_PATH=/app/data/app.db
